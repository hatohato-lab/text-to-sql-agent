# 説明書 — text-to-sql-agent

この文書は、IT や AI の専門知識がない方に向けて、このリポジトリ（プログラム一式）が
何をするものかを説明するものです。専門用語はできるだけ使わず、必要な言葉は最後の
「用語の小辞典」にまとめています。

## 一言でいうと

「日本語の質問をデータベースへの命令文（SQL）に書き換える AI」と、
「その書き換えが正しいかを自動で採点するプログラム」の組み合わせです。

たとえば「社員は全部で何人いるか」という日本語の質問を、
コンピュータが理解できる命令文に直し、その命令文が正しいかどうかを機械的に確かめます。

## 何の役に立つのか

AI が書いた命令文は、見た目だけでは正しいかどうか判断できません。
同じ意味の命令文でも書き方が何通りもあるからです。

そこでこのリポジトリでは、「見た目」ではなく「実際に動かした結果」で採点します。
算数の答案にたとえると、途中の式の書き方はどうであれ、
最後の答えが合っているかどうかで丸を付ける方式です。

この「結果で採点する」仕組みがあると、次のことができるようになります。

- AI の書いた命令文を、人が目で見て確認しなくても、自動で合否判定できる。
- AI やプログラムを直したとき、壊れていないかをすぐに確かめられる。

このリポジトリは小さな見本ですが、考え方はそのまま実務の品質チェックに応用できます。

## 正しさをどう確かめているか

確かめ方は二段構えです。

1. **答え合わせ**
   小さな社員名簿のデータベースを用意し、「お手本の命令文」と「AI が書いた命令文」を
   両方実行します。出てきた結果が一致すれば合格、違えば不合格です。

2. **採点係そのものの点検（自己検証）**
   採点プログラム自体が壊れていたら、採点結果は信用できません。
   そこで「正しいお手本」と「わざと間違えた例（3種類）」をあらかじめ用意し、
   お手本は合格に、間違い例はすべて不合格になることを確認します。
   体温計を、健康な人と発熱した人の両方で試して動作を確かめるのと同じ考え方です。

この点検は `python eval/oracle.py --selftest` という命令で、いつでも実行できます。

なお、この採点方式にも限界はあります（結果の列の並び順に影響される等）。
詳しくは README の「既知の限界」に記載しています。

## フォルダとファイルの案内

| 場所 | 中身 |
|---|---|
| `README.md` | 全体の説明（図つき）。まず読むファイル |
| `説明書.md` | この文書。専門用語を使わない説明 |
| `.claude/agents/text-to-sql-agent.md` | AI への指示書（何をどう作るかの依頼文） |
| `eval/oracle.py` | 採点プログラム本体 |
| `eval/corpus/reference.py` | お手本の命令文集 |
| `eval/corpus/broken_～.py` | わざと間違えた例（採点係の点検用） |
| `design/design.md` | なぜこの作りにしたかの設計メモ |

## 用語の小辞典

- **SQL（エスキューエル）** … データベースに「これを調べて」と頼むための命令文の言語。
- **データベース** … 表の形でデータをためておく仕組み。ここでは小さな社員名簿を使う。
- **オラクル** … 出力の合否を決める採点係のこと。この分野での慣用的な呼び名。
- **エージェント** … 指示を受けて作業を進める AI のこと。ここでは SQL を書く役。
- **自己検証（セルフテスト）** … 採点係自身が正しく働くかを、正解例と間違い例で確かめること。
